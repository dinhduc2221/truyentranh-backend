# Shared DB on 123host (use same values for local and Railway backend)
DB_HOST=free02.123host.vn
DB_PORT=3306
DB_NAME=dnpcvtnt_truyentranh
DB_USER=dnpcvtnt_layla
DB_PASS=Anhyeuem:)010203

# Optional: set true if your DB provider requires SSL
DB_SSL=false

JWT_SECRET=change_this_secret
PORT=3001

# Railway: put the same DB_* values above into Railway Variables.
# Optional (if provider gives DSN): DATABASE_URL=